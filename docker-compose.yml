services:
  # RTSP Server - 串流伺服器中心
  rtsp-server:
    image: bluenviron/mediamtx:latest
    restart: unless-stopped
    ports:
      - "8554:8554"   # RTSP 埠
      - "8888:8888"   # HLS 埠 (可選用)
      - "8889:8889"   # WebRTC 埠 (可選用)
    volumes:
      - ./mediamtx/mediamtx.yml:/mediamtx.yml:ro
    environment:
      - MTX_HLSVARIANT=fmp4
      - MTX_HLSALLOWORIGINS=*
      - MTX_WEBRTC=yes
    networks:
      - app-network

  # Stream Simulator - 模擬攝影機推流
  stream-simulator:
    image: linuxserver/ffmpeg:latest
    container_name: stream-simulator
    restart: unless-stopped
    depends_on:
      - rtsp-server
    volumes:
      - ../video:/videos:ro  # 掛載實體影片路徑
    networks:
      - app-network
    # 推流到 /simulator，避免覆蓋監控用的 /live（/live 改由外部 RTSP 拉流）
    command: >
      -re -stream_loop -1 
      -i "/videos/門禁遮臉入場/Video_衛哨端出入口.avi" 
      -vf "scale=426:240,fps=10,setpts=N/(10*TB)"
      -c:v libx264 -preset ultrafast -tune zerolatency -profile:v baseline -level 3.0 -g 20 -b:v 200k -maxrate 200k -bufsize 400k
      -bsf:v h264_mp4toannexb
      -an
      -f rtsp 
      -rtsp_transport tcp
      rtsp://rtsp-server:8554/simulator

  # Ollama Service
  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    # ports:
    #   - "11434:11434"  # 移除端口映射，避免衝突。backend 可通過服務名 ollama:11434 訪問
    environment:
      - OLLAMA_MAX_LOADED_MODELS=1
      # -1=永久預載；0=不預載（關閉後即卸載）；正數=閒置 N 分鐘後卸載。要省 GPU 可設 0
      - OLLAMA_KEEP_ALIVE=${OLLAMA_KEEP_ALIVE:--1}
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - app-network
    extra_hosts:
      - "registry.ollama.ai:104.16.55.3"  # 加這行，直接用 IP
      - "registry.ollama.ai:104.16.54.3"  # Cloudflare 的備用 IP
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # PostgreSQL Database with pgvector extension
  postgres:
    image: pgvector/pgvector:pg15  # 使用包含 pgvector 的官方映像
    restart: unless-stopped
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgAdmin - PostgreSQL 圖形化管理工具
  pgadmin:
    image: dpage/pgadmin4:latest
    restart: unless-stopped
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@admin.com
      - PGADMIN_DEFAULT_PASSWORD=admin
      - PGADMIN_CONFIG_SERVER_MODE=False
      - PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED=False
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    ports:
      - "5050:80"
    networks:
      - app-network
    depends_on:
      postgres:
        condition: service_healthy

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      network: host
    restart: unless-stopped
    shm_size: "2gb"  # 電腦視覺/多 worker 需較大 shared memory
    deploy:
      resources:
        limits:
          memory: 16G
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      - PROJECT_ROOT=${PROJECT_ROOT}
      - HOST=${HOST}
      - PORT=8080
      - RELOAD=${RELOAD:-false}
      # 每個 worker 為獨立 process，各載一份 Qwen+YOLO+ReID（約 17GB/process）。要改多 worker 請改下面為 WORKERS=2 或改用 ${WORKERS:-1} 並在 .env 設 WORKERS
      # - WORKERS=4
      - WORKERS=1
      - PRELOAD_CLIP=${PRELOAD_CLIP:-false}
      - PRELOAD_YOLO_REID=${PRELOAD_YOLO_REID:-true}
      - PRELOAD_SENTENCE_TRANSFORMER=${PRELOAD_SENTENCE_TRANSFORMER:-true}
      - LOG_LEVEL=${LOG_LEVEL}
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      - SESSION_TTL_SEC=${SESSION_TTL_SEC}
      - AUTO_RAG_INDEX=true
      - RAG_DIR=./rag_store
      - RAG_STORE_DIR=./rag_store
      - OLLAMA_EMBED_MODEL=bge-m3
      - OLLAMA_LLM_MODEL=qwen2.5vl:latest
      - OLLAMA_BASE=http://ollama:11434
      - OLLAMA_REQUEST_TIMEOUT=600
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - MY_API_KEY=${MY_API_KEY}
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - MOONDREAM_MODELS_DIR=/app/models
      # 離線或無法下載時：先在本機執行 mkdir -p yolo_weights && wget -O yolo_weights/yolov8s-world.pt https://github.com/ultralytics/assets/releases/download/v8.4.0/yolov8s-world.pt
      - YOLO_WEIGHTS_PATH=/app/yolo_weights/yolov8s-world.pt
      # Moondream 等 trust_remote_code 會寫入 cache；若主機 cache 唯讀，改寫到容器內可寫路徑
      # 與下方 volume 對應，讓本機已下載的 Qwen 被容器使用
      - HF_HOME=/root/.cache/huggingface
      - TRANSFORMERS_CACHE=/root/.cache/huggingface
      # 容器無外網：huggingface_hub 與 transformers 皆不連線
      - TRANSFORMERS_OFFLINE=1
      - HF_HUB_OFFLINE=1
      - QWEN_25VL_SNAPSHOT_PATH=${QWEN_25VL_SNAPSHOT_PATH:-}
      - QWEN_USE_QUANTIZATION=1
      # TorchAO 量化與 torch.compile 衝突會導致 meta/cuda 錯誤，從 process 啟動就停用 dynamo
      - TORCHDYNAMO_DISABLE=1
    volumes:
      - ./models:/app/models
      - ./yolo_weights:/app/yolo_weights
      - backend_hf_cache:/app/.cache/huggingface
      - ./backend/src:/app/src:delegated
      - ./backend/segment:/app/segment
      - ./backend/rag_store:/app/rag_store
      - ./backend/prompts:/app/prompts
      - ../video:/app/video:ro
      # 本機 HF cache：用與 download 腳本相同使用者的 $HOME，或於 .env 設 HF_CACHE_HOST=/mnt/10THDD/M133040024/.cache/huggingface
      - ${HF_CACHE_HOST:-${HOME}/.cache/huggingface}:/root/.cache/huggingface
    dns:
      - 8.8.8.8
      - 8.8.4.4
    # ports:
    #   - "8080:8080"  # 移除對外暴露，改由 nginx 反向代理
    networks:
      - app-network
    depends_on:
      postgres:
        condition: service_healthy
      ollama:
        condition: service_started
      rtsp-server: # 確保 backend 知道 rtsp-server 存在
        condition: service_started
    healthcheck:
      # 當 worker 全死導致 API 無回應時，healthcheck 失敗會觸發容器重啟
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=10)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      network: host
    restart: unless-stopped
    environment:
      # 不設置 REACT_APP_API_BASE，讓前端根據 hostname 自動選擇
      # - REACT_APP_API_BASE=http://localhost:8080  # 已移除，改為自動檢測
      - WDS_SOCKET_HOST=localhost
      - WDS_SOCKET_PORT=3000
    volumes:
      - ./frontend:/app:delegated
      # - node_modules_data:/app/node_modules
    dns:
      - 8.8.8.8
      - 8.8.4.4
    # ports:
    #   - "3000:3000"  # 移除對外暴露，改由 nginx 反向代理
    networks:
      - app-network
    depends_on:
      - backend

  # Nginx Reverse Proxy - 統一入口
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "3000:80"      # 廠商開放 3000，所以 Nginx 監聽 3000（映射到容器內部的 80）
      # - "8080:80"    # 備用：如果廠商也開放 8080，可以同時監聽兩個端口
    networks:
      - app-network
    depends_on:
      - backend
      - frontend

networks:
  app-network:
    driver: bridge

volumes:
  node_modules_data:
  ollama_data:
  postgres_data:
  pgadmin_data:
  backend_hf_cache:

